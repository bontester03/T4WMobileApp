<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="T4sV1.Views.MeasurementPage"
             xmlns:viewmodels="clr-namespace:T4sV1.Model.ViewModels"
             xmlns:measurement="clr-namespace:T4sV1.Model.Measurement"
             x:DataType="viewmodels:MeasurementViewModel"
             Title="Measurements"
             BackgroundColor="#F5F5F5">

    <Grid RowDefinitions="Auto,*" Padding="0">

        <!-- Loading Indicator -->
        <ActivityIndicator Grid.Row="0"
                          IsRunning="{Binding IsLoading}"
                          IsVisible="{Binding IsLoading}"
                          Color="#6200EE"
                          VerticalOptions="Center"
                          HorizontalOptions="Center"
                          Margin="20"/>

        <ScrollView Grid.Row="1" Padding="16">
            <ScrollView.Background>
                <!-- StartPoint defaults to (0,0) -->
                <LinearGradientBrush EndPoint="0,1">
                    <GradientStop Color="#e9fdb6" Offset="0.1" />
                    <GradientStop Color="#9dc052" Offset="1.0" />
                </LinearGradientBrush>
            </ScrollView.Background>
            <VerticalStackLayout Spacing="16">

                <!-- Error Message -->
                <Frame IsVisible="{Binding HasError}"
                       BackgroundColor="#FFEBEE"
                       BorderColor="#F44336"
                       CornerRadius="8"
                       Padding="12">
                    <Label Text="{Binding ErrorMessage}"
                           TextColor="#D32F2F"
                           FontSize="14"/>
                </Frame>

                <!-- No Data Message -->
                <Frame IsVisible="{Binding NoDataAvailable}"
                       BackgroundColor="White"
                       BorderColor="#E0E0E0"
                       CornerRadius="12"
                       Padding="20">
                    <VerticalStackLayout Spacing="12" HorizontalOptions="Center">
                        <Label Text="📏"
                               FontSize="48"
                               HorizontalOptions="Center"/>
                        <Label Text="No Measurements Yet"
                               FontSize="18"
                               FontAttributes="Bold"
                               HorizontalOptions="Center"
                               TextColor="#333"/>
                        <Label Text="Add your first measurement to track growth"
                               FontSize="14"
                               TextColor="#666"
                               HorizontalOptions="Center"
                               HorizontalTextAlignment="Center"/>
                        <Button Text="Add Measurement"
                                Command="{Binding AddMeasurementCommand}"
                                BackgroundColor="#6200EE"
                                TextColor="White"
                                CornerRadius="8"
                                Margin="0,12,0,0"/>
                    </VerticalStackLayout>
                </Frame>

                <!-- Latest Measurement Card -->
                <Frame IsVisible="{Binding HasData}"
                       BackgroundColor="White"
                       BorderColor="#E0E0E0"
                       CornerRadius="12"
                       Padding="20">
                    <VerticalStackLayout Spacing="16">
                        <Grid ColumnDefinitions="*,Auto">
                            <Label Grid.Column="0"
                                   Text="Latest Measurement"
                                   FontSize="20"
                                   FontAttributes="Bold"
                                   TextColor="#333"/>
                            <Label Grid.Column="1"
                                   Text="{Binding DateRecordedDisplay}"
                                   FontSize="12"
                                   TextColor="#999"
                                   VerticalOptions="Start"/>
                        </Grid>

                        <!-- Weight & Height -->
                        <Grid ColumnDefinitions="*,*" ColumnSpacing="16">
                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                <Label Text="⚖️ Weight"
                                       FontSize="14"
                                       TextColor="#666"/>
                                <Label Text="{Binding WeightDisplay}"
                                       FontSize="28"
                                       FontAttributes="Bold"
                                       TextColor="#6200EE"/>
                            </VerticalStackLayout>
                            <VerticalStackLayout Grid.Column="1" Spacing="4">
                                <Label Text="📏 Height"
                                       FontSize="14"
                                       TextColor="#666"/>
                                <Label Text="{Binding HeightDisplay}"
                                       FontSize="28"
                                       FontAttributes="Bold"
                                       TextColor="#6200EE"/>
                            </VerticalStackLayout>
                        </Grid>

                        <BoxView HeightRequest="1" BackgroundColor="#E0E0E0"/>

                        <!-- BMI & Health Range -->
                        <Grid ColumnDefinitions="*,*" ColumnSpacing="16">
                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                <Label Text="BMI"
                                       FontSize="14"
                                       TextColor="#666"/>
                                <Label Text="{Binding BMIDisplay}"
                                       FontSize="18"
                                       FontAttributes="Bold"
                                       TextColor="#333"/>
                            </VerticalStackLayout>
                            <VerticalStackLayout Grid.Column="1" Spacing="4">
                                <Label Text="Health Range"
                                       FontSize="14"
                                       TextColor="#666"/>
                                <Label Text="{Binding HealthRangeDisplay}"
                                       FontSize="18"
                                       FontAttributes="Bold"
                                       TextColor="#4CAF50"/>
                            </VerticalStackLayout>
                        </Grid>

                    </VerticalStackLayout>
                </Frame>

                <!-- History Section -->
                <Frame IsVisible="{Binding HasData}"
                       BackgroundColor="White"
                       BorderColor="#E0E0E0"
                       CornerRadius="12"
                       Padding="20">
                    <VerticalStackLayout Spacing="12">
                        <Grid ColumnDefinitions="*,Auto">
                            <Label Grid.Column="0"
                                   Text="Measurement History"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="#333"/>
                            <Button Grid.Column="1"
                                    Text="+ Add"
                                    Command="{Binding AddMeasurementCommand}"
                                    BackgroundColor="#6200EE"
                                    TextColor="White"
                                    CornerRadius="6"
                                    Padding="12,6"/>
                        </Grid>

                        <CollectionView ItemsSource="{Binding Measurements}"
                                       EmptyView="No measurements recorded yet">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="measurement:MeasurementDto">
                                    <Frame Margin="0,4" 
                                           BackgroundColor="#F9F9F9"
                                           BorderColor="#E0E0E0"
                                           CornerRadius="8"
                                           Padding="12">
                                        <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="8">
                                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                                <Label Text="{Binding DateRecorded, StringFormat='{0:MMM dd, yyyy}'}"
                                                       FontSize="14"
                                                       FontAttributes="Bold"
                                                       TextColor="#333"/>
                                                <Label Text="{Binding Weight, StringFormat='Weight: {0:F1} kg'}"
                                                       FontSize="12"
                                                       TextColor="#666"/>
                                                <Label Text="{Binding Height, StringFormat='Height: {0:F1} cm'}"
                                                       FontSize="12"
                                                       TextColor="#666"/>
                                            </VerticalStackLayout>
                                            <Button Grid.Column="1"
                                                    Text="Edit"
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MeasurementViewModel}}, Path=EditMeasurementCommand}"
                                                    CommandParameter="{Binding .}"
                                                    BackgroundColor="#03A9F4"
                                                    TextColor="White"
                                                    CornerRadius="6"
                                                    Padding="8,4"
                                                    FontSize="12"/>
                                            <Button Grid.Column="2"
                                                    Text="Delete"
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MeasurementViewModel}}, Path=DeleteMeasurementCommand}"
                                                    CommandParameter="{Binding .}"
                                                    BackgroundColor="#F44336"
                                                    TextColor="White"
                                                    CornerRadius="6"
                                                    Padding="8,4"
                                                    FontSize="12"/>
                                        </Grid>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Frame>

                <!-- Action Buttons -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="12" Margin="0,8">
                    <Button Grid.Column="0"
                            Text="🔄 Refresh"
                            Command="{Binding RefreshCommand}"
                            BackgroundColor="#4CAF50"
                            TextColor="White"
                            CornerRadius="8"/>
                    <Button Grid.Column="1"
                            Text="← Back"
                            Command="{Binding GoBackCommand}"
                            BackgroundColor="#9E9E9E"
                            TextColor="White"
                            CornerRadius="8"/>
                </Grid>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
